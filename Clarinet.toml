[project]
name = "hybrid-factory-machine"
authors = ["Stacks Developer"]
description = "A decentralized smart contract system for managing collaborative factory production"
telemetry = false
requirements = []
[contracts.production-manager]
path = "contracts/production-manager.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
